services:
  - type: web
    name: drone-dashboard-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: FRONTEND_URL
        value: https://drone-dashboard-eight.vercel.app
      - key: NODE_ENV
        value: production
